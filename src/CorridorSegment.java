import segmentswork.SegmentsConstants;

public class CorridorSegment extends Segment {
    public CorridorSegment(MapGame mapGame) {
        super(SegmentsConstants.CORRIDOR_DESCRIPTION, mapGame);
    }
}
